{
  "name": "node-example",
  "version": "1.0.0",
  "description": "This example is so cool.",
  "main": "cd/server app.js",
  "scripts": {
    "start": "npm start --prefix server",
    "build": "npm install --prefix server && npm install --prefix app && npm run build --prefix app && rm -rf server/public/* && cp -a app/build/. server/public/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "example",
    "heroku"
  ],
  "author": "jane-doe",
  "license": "MIT",
  "dependencies": {
  },
  "engines": {
    "node": "10.x"
  }
}